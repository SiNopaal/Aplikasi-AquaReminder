name: aquareminder
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Tambahan dependency
  flutter_local_notifications: ^17.2.4
  permission_handler: ^12.0.1
  sqflite: ^2.4.2
  intl: ^0.20.1
  timezone: ^0.9.4
  fl_chart: ^0.70.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Tambahkan ini jika kamu punya gambar atau file lokal lainnya
  # assets:
  #   - assets/images/
